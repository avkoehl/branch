# branch


## Installation

```bash
pip install git+https://github.com/avkoehl/branch.git
```

With development dependencies:

```bash
git clone https://github.com/avkoehl/branch.git
cd branch
uv sync --extra dev
```

install jupyter kernel:
```bash
uv run python -m ipykernel install --user --name=branch
```
